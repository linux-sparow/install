#!/bin/bash

## PACKAGE 
PACKAGE="${PACKAGE} base base-devel linux-firmware sof-firmware mkinitcpio mesa ttf-droid kitty-terminfo bash-completion git wget aria2 unzip neovim flatpak fuse btop less "


## DAEMONS
RDAEMON="${RDAEMON} update.timer "


## CONFIGS
if [[ -d "$appdir/library/install/system/basics/config" ]]; then
    cp -fr "$appdir/library/install/system/basics/config/"* "$appdir/config/define/"
fi