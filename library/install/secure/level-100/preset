#!/bin/bash

## PACKAGE 
PACKAGE="${PACKAGE} keepassxc firewalld iptables-nft"


## DAEMONS
RDAEMON="${RDAEMON} firewalld"


## CONFIGS
if [[ -d "$appdir/library/install/secure/level-0/config" ]]; then
    cp -fr "$appdir/library/install/secure/level-0/config/"* "$appdir/config/define/"
fi